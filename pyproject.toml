[project]
name = "novel-app"
version = "0.1.0"
description = "小说下载阅读器"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.28.0",
    "lxml>=4.9.0",
    "curl-cffi>=0.15.0",
]
